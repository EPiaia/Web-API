CREATE DATABASE WEBAPI;

USE WEBAPI;


CREATE TABLE PRODUCT (
	P_ID INT NOT NULL,
	P_DESC VARCHAR(300) NOT NULL,
    P_MARCA VARCHAR(200) NOT NULL,
    P_VALOR_UN DECIMAL(7,2) NOT NULL,
    PRIMARY KEY (P_ID)
);

INSERT INTO PRODUCT VALUES (1, 'CANO 20mm', 'AMANCO', 17.50);
